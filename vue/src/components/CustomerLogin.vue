<template>
  <!-- 会员客户登录/注册页面的根元素，使用类名来应用相应的样式 -->
  <div class="customer-login-page">
    <!-- 展示欢迎文字的图片，通过src属性引入项目中的资源，class用于样式设置，alt为图片无法显示时的替代文本 -->
    <img src="@/assets/welcome_text.png" class="welcome-image" alt="Welcome Text" />  
    <!-- 展示会员客户标题的图片，同样通过相关属性进行资源引入、样式设置以及替代文本定义 -->
    <img src="@/assets/customertitle.png" class="customer-title" alt="Customer Title" />
    <!-- 页面中的二级标题元素，用于提示这部分是会员客户登录/注册相关功能 -->
    <h2>会员客户登录/注册</h2>
    <!-- 登录按钮，绑定了click事件，点击时会触发goToLogin方法，设置了相应的样式类 -->
    <button @click="goToLogin" class="magic-button">登录</button>
    <!-- 注册按钮，绑定了click事件，点击时会触发goToRegister方法，设置了相应的样式类 -->
    <button @click="goToRegister" class="magic-button">注册</button>
  </div>
</template>

<script>
export default {
  methods: {
    // 定义登录按钮点击后的操作方法，使用Vue Router进行页面导航，将页面跳转到/customer-login/login路径对应的页面（假设该路径已在路由配置中定义好）
    goToLogin() {
      this.$router.push("/customer-login/login");
    },
    // 定义注册按钮点击后的操作方法，同样使用Vue Router进行页面导航，跳转到/customer-login/register路径对应的页面（假设该路径已在路由配置中定义好）
    goToRegister() {
      this.$router.push("/customer-login/register");
    }
  }
};
</script>

<style scoped>
/* 欢迎文字图片的样式设置 */
.welcome-image {
  /* 使用绝对定位，方便精确调整图片在页面中的位置，使其不受文档流常规布局的影响 */
  position: absolute; /* 使用绝对定位 */
  /* 设置图片距离页面顶部的距离为20px，确定其垂直方向上的位置 */
  top: 20px; /* 距离顶部的距离 */
  /* 设置图片距离页面右侧的距离为20px，确定其水平方向上的位置 */
  right: 20px; /* 距离右侧的距离 */
  /* 让图片宽度根据自身内容自动调整大小，保持图片原始比例显示 */
  width: auto; /* 根据内容自动调整宽度 */
  /* 设置图片的最大宽度为600px，防止图片因尺寸过大而破坏页面整体布局效果 */
  max-width: 600px; /* 最大宽度 */
}

/* 会员客户标题图片的样式设置 */
.customertitle{
  /* 设置图片顶部和底部的外边距为40px，使图片在垂直方向上与周边元素有一定间隔，布局更美观 */
  margin: 40px 0; /* 顶部和底部的外边距 */
  /* 将图片宽度设置为占满父容器的40%，可根据实际页面布局需求调整此比例，使其在水平方向上占据合适宽度 */
  width: 40%; /* 使标题宽度占满 */
  /* 让图片内的文本在水平方向上居中显示，提升视觉效果 */
  text-align: center; /* 文本居中 */
}

/* 会员客户登录/注册页面整体的样式设置 */
.customer-login-page {
  /* 使页面内的文本内容在水平方向上居中对齐，保持整体布局的整齐性 */
  text-align: center;
  /* 设置页面的背景图片，通过相对路径引用项目中assets目录下的图片资源（这里的路径写法‘~@/assets/’可能是基于项目构建工具的特定写法来准确指向资源位置） */
  background-image: url('~@/assets/cgb.jpg'); /* 背景图 */
  /* 背景图片尺寸设置为覆盖整个页面容器，保证背景图能完整铺满整个页面可视区域 */
  background-size: cover; /* 背景图覆盖 */
  /* 背景图片在页面容器中居中显示，确保背景图展示效果均匀、美观 */
  background-position: center; /* 背景图居中 */
  /* 设置页面最小高度为视口高度（100vh表示浏览器可视区域的高度），确保页面内容在垂直方向上至少能占满整个屏幕高度 */
  min-height: 100vh; /* 至少占满视口高度 */
  /* 使用flex布局，将页面内的子元素按垂直方向排列，方便进行整体的布局排版 */
  display: flex;
  flex-direction: column;
  /* 在垂直方向上对页面内的子元素进行居中对齐，使页面整体视觉效果更平衡 */
  justify-content: center;
  /* 在水平方向上对页面内的子元素进行居中对齐，保证各元素在页面中心位置展示 */
  align-items: center;
  /* 设置页面内文本的字体颜色为金色（#FFD700），营造出符合主题的魔法奇幻风格 */
  color: #FFD700; /* 字体颜色 */
  /* 设置页面容器内的内边距为20px，使页面内容与容器边缘有一定距离，避免内容过于贴近边缘影响美观度 */
  padding: 20px;
  /* 给页面容器设置边框圆角效果，半径为15px，使页面看起来更圆润、美观，有柔和的视觉感受 */
  border-radius: 15px;
  /* 给页面容器添加阴影效果，通过设置水平、垂直偏移量、模糊半径以及阴影颜色等参数，增加页面的立体感和层次感 */
  box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
}

/* 一级标题（h1标签）的样式设置，虽然此页面示例中未使用到h1，但作为样式定义保留在这里，可用于后续可能的扩展或统一风格调整 */
h1 {
  /* 设置字体大小为36px，可根据设计需求调整字号大小来突出标题效果 */
  font-size: 36px;
  /* 设置底部外边距为10px，使标题与下方元素有一定间隔，布局更清晰 */
  margin-bottom: 10px;
  /* 给标题文字添加阴影效果，通过设置水平、垂直偏移量、模糊半径以及阴影颜色等参数，增加文字的立体感和视觉突出度 */
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
}

/* 二级标题（h2标签）的样式设置，用于页面中“会员客户登录/注册”这个标题的样式呈现 */
h2 {
  /* 设置字体大小为24px，使其相较于一级标题字号稍小，体现标题层级关系 */
  font-size: 24px;
  /* 设置底部外边距为20px，与下方元素拉开一定距离，让页面布局更合理有序 */
  margin-bottom: 20px;
  /* 给标题文字添加阴影效果，通过设置相关参数增加文字的立体感和视觉效果，参数较一级标题的阴影效果稍弱，体现不同层级标题间的差异 */
  text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
}

/* 按钮（class为magic-button的按钮）的样式设置，用于登录和注册按钮的样式统一呈现 */
.magic-button {
  /* 设置按钮上下外边距为10px，使按钮之间以及按钮与周边元素在垂直方向上有合适间隔 */
  margin: 10px;
  /* 设置按钮内边距，使按钮内的文本与按钮边框有一定距离，提升按钮的视觉效果和可点击性 */
  padding: 10px 20px;
  /* 设置按钮上文本的字体大小为16px，确保文字大小适中、清晰可读 */
  font-size: 16px;
  /* 设置按钮的背景颜色为深紫色（#8B008B），营造出神秘、奇幻的风格色彩，符合页面整体主题氛围 */
  background-color: #8B008B; /* 深紫色 */
  /* 设置按钮文本颜色为白色，与深紫色背景形成鲜明对比，提高文字可读性 */
  color: white;
  /* 去除按钮的默认边框，使按钮外观更简洁，符合自定义的样式风格 */
  border: none;
  /* 设置按钮边框的圆角效果，半径为5px，让按钮边角更圆润，视觉上更美观、柔和 */
  border-radius: 5px;
  /* 给按钮添加阴影效果，通过设置相关参数使按钮在页面上呈现出立体感，增强视觉效果和交互感 */
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.6);
  /* 将鼠标指针样式设置为手型，提示用户该元素是可点击的，符合用户操作习惯和交互预期 */
  cursor: pointer;
  /* 设置按钮在鼠标悬停等交互操作时的过渡效果，包括背景颜色变化和按钮缩放变化，过渡时间分别为0.3秒和0.2秒，使交互过程更平滑、自然，增强用户体验 */
  transition: background-color 0.3s, transform 0.2s;
}

/* 按钮在鼠标悬停时的样式变化设置，通过:hover伪类选择器来定义 */
.magic-button:hover {
  /* 当鼠标悬停在按钮上时，改变按钮的背景颜色为淡紫色（#DA70D6），用于提示用户当前鼠标所在位置是可操作的按钮区域，同时也丰富了按钮的交互视觉效果 */
  background-color: #DA70D6; /* 悬停效果 */
  /* 对按钮进行放大操作，使其在悬停时有一定的视觉反馈，比如放大比例为1.05倍，增强按钮的交互感，让用户更直观地感受到操作反馈 */
  transform: scale(1.05); /* 按钮放大效果 */
}
</style>